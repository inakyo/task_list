<h1>ユーザ一覧</h1>

<%= link_to "タスク一覧に戻る", tasks_path%>

 <table>
    <tr>
      <th>氏名</th>
      <th>role</th>
    </tr>
    <% @users.each do |user|%>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.role %></td>
      </tr>
    <% end %>
  </table>